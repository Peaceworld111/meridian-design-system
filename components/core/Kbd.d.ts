/** Keyboard key glyph, used in back-office queue toolbars to advertise shortcuts. */
export interface KbdProps { children?: React.ReactNode }
export declare function Kbd(props: KbdProps): JSX.Element;
