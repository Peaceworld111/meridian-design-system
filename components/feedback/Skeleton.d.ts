/** Loading placeholder. Keep the real column widths and the real money font-size so the
 * layout does not jump when data lands. No shimmer animation. */
export interface SkeletonProps { w?: number | string; h?: number; style?: React.CSSProperties }
export declare function Skeleton(props: SkeletonProps): JSX.Element;
