Back-office queue header.

```jsx
<QueueToolbar title="出金复核" count={4} value={tab} onChange={setTab}
  tabs={['我的案件', '未认领', '全部']}
  shortcuts={[{ key: 'J', label: '下一条' }, { key: 'A', label: '放行' }]} />
```